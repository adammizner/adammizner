name: Status Banner Update

on:
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Debug trigger
        run: echo "Workflow is running correctly"
